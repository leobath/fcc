
function uniteUnique(arr) {
    var numberOfArguments =  arr.length;
    console.log(numberOfArguments);
    var i=0;
    var argument = [];
    while(i<arr.length){                 
        for (var j=0 in arr){
            console.log(arr);
        }
        i++;
        
    }
    console.log(argument);

    return arr;
  }
  
  uniteUnique([1, 3, 2], [5, 2, 1, 4], [2, 1]);
  