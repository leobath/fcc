function convertHTML(str) {
    // &colon;&rpar;
    var regex = /(\W+)\S(W+)/;
    

    switch (str) {
        case "&":
            str = str.replace
            break;
    
        default:
            break;
    }

    return str;
  }
  
  convertHTML("Dolce & Gabbana");
  